global:
  dist-dirs: &distDirs dist
  tsconfig: tsconfig.json

clean-pkg-json:
  sort-scripts: true

copy-files:
  from:
    - package.json
    - LICENSE
    - CHANGELOG.md
    - README.md
  to: *distDirs

build:
  entry: src/index.ts
  license-banner: true
  out: *distDirs
  umd-name: @aloreljs/rxutils
  externals:
    - tslib
    - rxjs
    - rxjs/operators
    - lodash
    - lodash/isEqual
    - lodash-es
